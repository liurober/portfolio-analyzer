---
description: Full equity analysis — technical (Murphy framework), fundamental, GEX, trade ideas, PDF + Obsidian
argument-hint: "<TICKER> — e.g. /analyze AAPL"
---

Use the Skill tool to invoke `equity-research:analyze` with the ticker from the argument.

**Rules — no exceptions:**
- Do NOT search the web
- Do NOT ask clarifying questions
- Do NOT summarize or produce a manual analysis
- Do NOT skip the Skill tool invocation

The ticker is the first word of the argument (e.g. `/analyze AAPL` → ticker is `AAPL`). Pass it as the `args` parameter to the `equity-research:analyze` skill and follow the skill's steps exactly.
